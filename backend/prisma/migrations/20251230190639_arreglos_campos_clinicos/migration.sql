BEGIN TRY

BEGIN TRAN;

-- AlterTable
ALTER TABLE [dbo].[Detalle_Campo_Clinico] ADD [Comuna] VARCHAR(100),
[Direccion_Establecimiento] VARCHAR(200),
[Region] VARCHAR(100);

COMMIT TRAN;

END TRY
BEGIN CATCH

IF @@TRANCOUNT > 0
BEGIN
    ROLLBACK TRAN;
END;
THROW

END CATCH
