BEGIN TRY

BEGIN TRAN;

-- AlterTable
ALTER TABLE [dbo].[Denuncia] ADD [Fecha_Ingreso] DATE NOT NULL CONSTRAINT [Denuncia_Fecha_Ingreso_df] DEFAULT CURRENT_TIMESTAMP;

COMMIT TRAN;

END TRY
BEGIN CATCH

IF @@TRANCOUNT > 0
BEGIN
    ROLLBACK TRAN;
END;
THROW

END CATCH
